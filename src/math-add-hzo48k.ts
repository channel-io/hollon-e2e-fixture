/**
 * Adds two numbers together.
 *
 * @param a - The first number to add
 * @param b - The second number to add
 * @returns The sum of a and b
 *
 * @example
 * ```typescript
 * addNumbershzo48k(2, 3) // returns 5
 * addNumbershzo48k(-1, 1) // returns 0
 * addNumbershzo48k(0.1, 0.2) // returns 0.30000000000000004
 * ```
 */
export function addNumbershzo48k(a: number, b: number): number {
  return a + b;
}
