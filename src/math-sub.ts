/**
 * Subtracts one number from another.
 *
 * @param a - First number (minuend)
 * @param b - Second number (subtrahend)
 * @returns The difference of a minus b
 */
export function subtractNumbersho5nxe(a: number, b: number): number {
  return a - b;
}
