/**
 * Subtracts the second number from the first number.
 *
 * @param a - The minuend (number to subtract from)
 * @param b - The subtrahend (number to subtract)
 * @returns The difference of a and b
 *
 * @example
 * ```typescript
 * subtractNumbershzo48k(10, 5); // returns 5
 * subtractNumbershzo48k(5, 10); // returns -5
 * subtractNumbershzo48k(0.3, 0.2); // returns ~0.1
 * ```
 */
export function subtractNumbershzo48k(a: number, b: number): number {
  return a - b;
}
