export function validateInputi3jb57(a: number, b: number): void {
  if (Number.isNaN(a)) {
    throw new Error('Parameter a is NaN');
  }
  if (Number.isNaN(b)) {
    throw new Error('Parameter b is NaN');
  }
}
